# Referência bbs_bodycam

O script completo foi integrado em:

```
PC/bbs_bodycam/
```

Este diretório mantém apenas o backup `bbs_bodycam.zip` enviado originalmente.

A conversão vRP foi feita nos arquivos abertos pelo BBS (`Framework/*.lua` e `config.lua`).
